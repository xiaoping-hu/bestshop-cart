apply plugin: "war"

bootWar {
    mainClassName = "hu.xiaoping.bestshop.cart.CartApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
